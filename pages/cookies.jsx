import { Layout } from '../components/SiteChrome';
import { Meta } from '../components/PageParts';
export async function getServerSideProps() { return { props: { enabled: process.env.CONVERSION_ANALYTICS_ENABLED === 'true' } }; }
export default function Cookies({ enabled }) {
  return <Layout><Meta title="Cookies y medición" description="Uso de cookies, registros técnicos y medición de conversión en AfincalIA."/><article className="content-page legal-page page-shell"><h1>Cookies y medición</h1><h2>Navegación de la web comercial</h2><p>El código de esta web no instala cookies de publicidad ni utiliza identificadores de seguimiento en almacenamiento local. El formulario y la demo mantienen su estado temporalmente en la página.</p><h2>Medición de solicitudes</h2><p>{enabled ? 'Medimos clics en solicitudes, apertura e inicio del formulario, confirmaciones y errores mediante contadores por día y página de origen. No enviamos nombres, emails, mensajes, identificadores de visitantes ni parámetros de las URL a estos contadores. Respetamos la señal Do Not Track del navegador.' : 'La medición de conversión está desactivada. No hay contadores de captación activos.'}</p><h2>Registros del alojamiento</h2><p>El alojamiento puede procesar datos técnicos de conexión para servir la web y mantener su seguridad. Estos registros son distintos de la medición comercial.</p><h2>Servicios externos</h2><p>WhatsApp y el correo se abren solo al pulsar sus enlaces. Sus condiciones de privacidad corresponden a cada proveedor. No se cargan dentro de esta página como herramientas de publicidad.</p></article></Layout>;
}
